using Cinemachine;
using UnityEngine;

public class CMHandler : MonoBehaviour
{
    //CinemachineBlendListCamera lastCMBlendList;

    //public void PlayCM(CinemachineBlendListCamera blendList)
    //{
    //    if (lastCMBlendList != null && lastCMBlendList.gameObject.activeInHierarchy)
    //        lastCMBlendList.gameObject.SetActive(false);

    //    blendList.gameObject.SetActive(true);
    //    blendList.ChildCameras[0].transform.position = Camera.main.transform.position;

    //    lastCMBlendList = blendList;
    //}
}
